---
project: OQupy_srv
date: 2026-06-01
time: 17:20
branch: dev
tags: [bugfix, feature, ci-cd, dependency, testing, config, security, investigation]
jira: []
---

## What was worked on
- Diagnosed Railway deployment failure: `postbuild` script was symlinking Prisma generated client using absolute Mac path (`/Users/I532880/Library/MINE/OQupy_srv/oqupy-srv/generated`), which doesn't exist in Railway's container
- Fixed `package.json` postbuild script: changed `ln -sf $(pwd)/generated dist/generated` to `cp -r generated dist/generated` to physically copy the Prisma client instead of symlinking
- Committed and pushed the `package.json` fix to trigger Railway redeploy
- Reviewed uncommitted changes in working tree (studios availability filtering, admin functionality, e2e tests) — left uncommitted pending separate review/commit
- Assessed Railway environment setup: confirmed `staging` environment exists but has no service deployed; agreed dev + prod is sufficient for current MVP stage

## Key decisions
- Commit deployment fix (`package.json`) separately from feature work (studio availability filtering) to unblock Railway immediately
- Delete or deprioritize `staging` environment — not needed until beta users exist in prod; dev + prod is the right setup for solo pre-launch

## Root cause / findings
- Railway builds in a clean container where absolute local paths don't resolve at runtime; symlinks created with `$(pwd)` fail because the path is hardcoded at build time but doesn't exist in the container filesystem
- `staging` environment was manually created in Railway but has never had a service deployed to it

## Open / next session
- Monitor Railway deployment dashboard to confirm `oqupy-srv / development` redeploy succeeds with the `cp -r` fix
- Decide on committing or discarding the in-progress studio availability filtering changes (studios.service.ts +108, admin.service.ts +69, studios.e2e-spec.ts +171)
- Consider explicitly pinning `engines.node` in `package.json` to avoid Node version surprises (Prisma 7.x requires Node 18+)
- Address outstanding infra setup items in `docs/todo.md`: Railway env vars, SES, Google OAuth prod creds, MSG91 DLT registration

## Key files touched
- `package.json` (postbuild script fix)
- `studios.service.ts` (uncommitted: availability filtering)
- `admin.service.ts` (uncommitted: new functionality)
- `studios.e2e-spec.ts` (uncommitted: new tests)
