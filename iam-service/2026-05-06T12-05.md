---
project: iam-service
date: 2026-05-06
time: 12:05
branch: feat/CALMSERVICES-58425-remove-replication-admin-rest-controller-clean
tags: [bugfix, feature, ci-cd, dependency, testing, config, security, investigation]
jira: [GROUP-1183]
---

## What was worked on
- Investigated "System Trust Session Token Retrieval Failed" warning on all scheduled pipeline runs for `iam-service`
- Verified `id-token: write` permission is correctly configured at job level in `piper-scheduled-pipeline.yml` (uses `sap-oss-ppms-workflow.yml`)
- Confirmed warning appears consistently across all nightly scheduled runs, not isolated to feature branch or PR pushes
- Checked for Hyperspace Portal bot PR in open and merged PRs — none found despite repo being onboarded
- Created pinned GitHub issue #3785 assigned to track System Trust registration failure
- Drafted Slack message to Piper/Compliance team for pipeline group `PIPELINE-GROUP-1183` registration in System Trust database

## Root cause / findings
- Pipeline group `PIPELINE-GROUP-1183` (used by `sap-oss-ppms-workflow.yml`) is not registered in the System Trust database
- Hyperspace Portal onboarding and System Trust pipeline registration are separate processes; repo is onboarded to Hyperspace but pipeline group metadata was never registered
- The warning is non-blocking by design — scheduled pipeline continues executing despite token retrieval failure
- This is a platform/infrastructure issue, not a code issue

## Open / next session
- Await response from Piper/Compliance team on Slack regarding System Trust registration of pipeline group `PIPELINE-GROUP-1183`
- Once registered, verify next scheduled run (23:40 UTC) completes without warning
- Close issue #3785 when resolved
