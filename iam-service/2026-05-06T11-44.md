---
project: iam-service
date: 2026-05-06
time: 11:44
branch: feat/CALMSERVICES-58425-remove-replication-admin-rest-controller-clean
tags: [bugfix, feature, refactor, ci-cd, dependency, testing, security, investigation]
jira: []
---

## What was worked on
- Implemented CALMSERVICES-58425: removed `ReplicationAdminRestController` and 8 exclusive service methods from `ReplicationAdminService` per ADR requirements
- Deleted `ReplicationAdminRestController.java`, `ReplicationAdminRestControllerTest.java`, and integration test `ReplicationAdminRestControllerTest.java`
- Removed methods: `queuedSanitySanityCheckEvent`, `sanityCheckQueuedTasks`, `sanityCheckIssues`, `sanityCheckActions`, `sanityCheckIssue`, `sanityCheckAction`, `isConsistent`, `resetMessageProcessing`, `fetchMessage` from `ReplicationAdminService`
- Removed `fetchMessage` method from `PublishReplicationHelper.java` (verified no other callers)
- Cleaned up corresponding unit tests in `ReplicationAdminServiceTest.java` and `PublishReplicationHelperTest.java`, retaining tests for `getStatusesOfPublishedEntities` and `publishEntities`
- Rebased feature branch onto clean `main` to isolate changes from unrelated `fix/dependabot-security-overrides` commits
- Created PR #3784 with only the 7 expected files (no `package.json` drift)

## Key decisions
- Rebased onto `main` instead of merging to keep `fix/dependabot-security-overrides` changes separate and maintain clean PR history

## Root cause / findings
- Initial branch was created off `fix/dependabot-security-overrides`, which included unrelated `mtx/sidecar/package.json` changes; rebasing onto `main` cleanly isolated CALMSERVICES-58425 changes

## Open / next session
- PR #3784 ready for review; old PR #3783 closed

## Key files touched
- `ReplicationAdminRestController.java` (deleted)
- `ReplicationAdminRestControllerTest.java` (deleted)
- `integration-tests/.../ReplicationAdminRestControllerTest.java` (deleted)
- `ReplicationAdminService.java` (modified: removed 9 methods, cleaned imports)
- `PublishReplicationHelper.java` (modified: removed `fetchMessage`)
- `ReplicationAdminServiceTest.java` (modified: removed 8 test methods)
- `PublishReplicationHelperTest.java` (modified: removed `fetchMessage` tests)
