---
project: iam-ui
date: 2026-05-05
time: 14:00
branch: hotfix/pin-sap-piper-1404-sdol025
tags: [hotfix, bugfix, refactor, ci-cd, dependency, testing, config]
jira: []
---

- Investigated PR #3019 failures on `hotfix/pin-sap-piper-1404-sdol025` branch — two separate build failures in `npmExecuteTests` and `npmExecuteScripts` jobs
- Root cause identified: `ignore-scripts=true` npm config change silently skipped `postinstall` hooks, preventing Puppeteer's Chromium binary download and breaking headless browser tests with `exit status 255`
- Reviewed Hyperspace's comment on PR confirming diagnosis — `preinstall` script became dead code and Puppeteer couldn't initialize without its binary
- Two follow-up commits already on branch address the issue: commit `7164de9270` restores Puppeteer Chromium download and removes dead `preinstall` hook; commit `03570bd56f` adds experimental npm config flag
- Next step: validate that these fixes resolve the test runner crash and both CI jobs pass on rebuild

### Files with uncommitted changes
—
