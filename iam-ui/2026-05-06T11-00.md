---
project: iam-ui
date: 2026-05-06
time: 11:00
branch: hotfix/pin-piper-pipeline-github-v1.56.2
tags: [hotfix, bugfix, refactor, ci-cd, dependency, testing, config, investigation]
jira: []
---

I'll save this session to the Obsidian vault.

---

## What was worked on
- Fixed `/boot` command to auto-fetch session notes without permission prompts — switched from `ls` to `find` with quoted paths to avoid backslash-escaped whitespace safety checks
- Created `boot-session-files.sh` wrapper script to handle variable expansion internally, so Claude runs a static command that matches the allow rule exactly
- Updated `settings.json` allow rules to match the script path without ambiguity
- Updated `boot.md` prompt to request compact 4-line summary (branch, last worked on, open items, watch-outs)
- Investigated Jira MCP tool configuration — updated `.jira-config.json` in `dist/` directory with correct `projectKey` format to enable ticket creation

## Key decisions
- Use a wrapper script instead of inline variable expansion — cleaner allow rules, avoids Claude Code safety gates firing on `$PROJECT` variables
- Keep `/boot` output to ~4 lines max — skip empty sections to stay scannable

## Root cause / findings
- Claude Code's safety checks fire **before** allow rule matching on commands containing shell variables (`$PROJECT`) or backslash-escaped whitespace (`Claude\ Sessions`) — allow rules alone can't bypass these
- The `sap-jira` MCP tool reads `.jira-config.json` from its own `process.cwd()` (the `dist/` directory where it launches), not from the current working directory

## Open / next session
- Create Jira ticket for pipeline version pins (`sap-piper` + `piper-pipeline-github` hotfix) — confirm whether it should go under `CALMSERVICES-60965` parent or be standalone
- DWC Release stage timeout (`sapDwCStageRelease`, run #33941307) still unresolved — infrastructure-side, may need escalation
- Confirm whether `PostCompact` Obsidian hook should be removed entirely

## Key files touched
- `settings.json` — allow rules for `find` command and boot script
- `boot.md` — tightened prompt, now calls external script instead of inline command
- `boot-session-files.sh` — new wrapper script in `~/.claude/scripts/`
- `dist/.jira-config.json` — corrected `projectKey` format

---

**Session saved.**

Now back to the Jira question: should the pipeline fix ticket go under `CALMSERVICES-60965` (Replication Failures parent) or be separate?
